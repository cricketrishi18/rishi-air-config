server:
  port: 5005

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/airline_pricing_service
    username: root
    password: Rishi@18

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
server:
  port: 5011

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/airline_flight_ops_service
    username: root
    password: Rishi@18

  kafka:
    bootstrap-servers: localhost:9092

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
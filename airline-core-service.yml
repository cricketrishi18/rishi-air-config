server:
  port: 5002

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/airline_core_service
    username: root
    password: Rishi@18
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true